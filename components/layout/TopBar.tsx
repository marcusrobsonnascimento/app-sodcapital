'use client'

// TopBar removido para aproveitar mais espaço na tela
// O título da página ativa já aparece na aba do PanelContainer

export default function TopBar() {
  // Retorna null para não renderizar nada
  return null
}